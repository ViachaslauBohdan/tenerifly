/**
 * Tenerife Joy collaborator id assigned by Atlántico Excursiones.
 * Website links use it as `afId`; the supplier API uses it as `userId` / office.
 */
export const ATLANTICO_AFFILIATE_ID = "3726";
